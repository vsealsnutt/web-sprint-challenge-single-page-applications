describe('BloomTech Eats App', () => {
    beforeEach(() => {
        cy.visit('http://localhost:3000');
    })

    const nameInput = () => cy.get('');
    const toppingSelect = () => cy.get('');
    const submitBtn = () => cy.get(``);

    it('the proper elements are showing', () => {
        
    })





})